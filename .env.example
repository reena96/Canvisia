# ============================================
# CANVISIA - ENVIRONMENT VARIABLES TEMPLATE
# ============================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Fill in your actual values in .env.local
#
# 3. NEVER commit .env.local to git!
#    (It's already in .gitignore)
#
# ============================================

# ============================================
# FIREBASE CONFIGURATION
# ============================================
# Get these from: Firebase Console > Project Settings > General > Your apps > Web app
#
# IMPORTANT: Firebase web API keys are safe to expose in client-side code.
# They identify your Firebase project but cannot be used to access your data
# without proper security rules. See: https://firebase.google.com/docs/projects/api-keys

VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================
# ANTHROPIC CLAUDE API
# ============================================
# Get your API key from: https://console.anthropic.com/settings/keys
#
# ⚠️ SECURITY WARNING:
# DO NOT expose this in client-side code!
# This should only be used in server-side code or API routes.

VITE_CLAUDE_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# APPLICATION SETTINGS
# ============================================

# Environment (development, staging, production)
VITE_ENV=development

# App URL (for redirects, CORS, etc.)
VITE_APP_URL=http://localhost:5173

# Enable debug logging
VITE_DEBUG=true

# ============================================
# FIREBASE EMULATOR (for testing)
# ============================================
# Uncomment these when running with Firebase emulators
# See: npm run emulators

# VITE_USE_EMULATOR=true
# VITE_FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
# VITE_FIREBASE_FIRESTORE_EMULATOR_HOST=localhost:8080
# VITE_FIREBASE_RTDB_EMULATOR_HOST=localhost:9000

# ============================================
# DEPLOYMENT SECRETS (Vercel Environment Variables)
# ============================================
# These should be set in Vercel dashboard, not in .env files:
#
# Production Firebase Config:
#   - VITE_FIREBASE_API_KEY
#   - VITE_FIREBASE_AUTH_DOMAIN
#   - VITE_FIREBASE_PROJECT_ID
#   - (etc.)
#
# Claude API Key (server-side only):
#   - CLAUDE_API_KEY (NOT prefixed with VITE_)
#
# See: https://vercel.com/docs/concepts/projects/environment-variables
