{
  "rules": {
    "cursors": {
      "$canvasId": {
        ".read": true,
        "$userId": {
          ".write": "$userId === auth.uid"
        }
      }
    },
    "presence": {
      "$canvasId": {
        ".read": true,
        "$userId": {
          ".write": "$userId === auth.uid"
        }
      }
    },
    "connections": {
      "$canvasId": {
        ".read": true,
        "$userId": {
          ".write": "$userId === auth.uid"
        }
      }
    }
  }
}
